<svg viewBox="0 0 424 400" xmlns="http://www.w3.org/2000/svg">
  <!-- Left vertical rectangle (dark) -->
  <rect x="0" y="0" width="78" height="400" fill="#1a4d4d"/>

  <!-- Main diagonal stripe (dark) -->
  <polygon points="78,0 178,0 78,240" fill="#1a4d4d"/>

  <!-- Top right horizontal bar (bright) -->
  <rect x="120" y="0" width="304" height="78" fill="#4ECDC4"/>

  <!-- Bottom right square (bright) -->
  <rect x="178" y="220" width="246" height="180" fill="#4ECDC4"/>

  <!-- Small light triangle accent -->
  <polygon points="120,250 178,220 178,300" fill="#60D394"/>
</svg>
